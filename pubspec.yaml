name: portfolio
description: 심진웅 — Mobile App Developer 포트폴리오 (Jaspr / SSG)
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.10.0

dependencies:
  jaspr: ^0.22.4
  jaspr_router: ^0.8.1

dev_dependencies:
  build_runner: ^2.10.0
  build_web_compilers: ^4.8.0
  jaspr_builder: ^0.22.4
  lints: ^5.0.0

jaspr:
  mode: static
